services:
  - type: web
    name: jv-fitting
    runtime: python
    plan: starter
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn --timeout 180 app:app
    envVars:
      - key: PYTHON_VERSION
        value: "3.11.8"
